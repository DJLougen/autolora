# secrets — MUST stay untracked
.env
.env.*
**/vault/
*vault*
secrets/

# the agent's OWN exported traces — private, never commit
**/data/traces_raw/
**/data/train_dataset.jsonl
**/data/incumbent_scores.json

# audit log + run artifacts
**/swaps.jsonl
**/adapters/
**/serve/live_adapter/
**/serve/active.json
**/serve/HANDOFF.md

# python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/
